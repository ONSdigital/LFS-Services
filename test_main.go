package main

import (
	"testing"
)

const AddressURL = "http://localhost:8000/address"

func TestImportAddress(t *testing.T) {

	//hc := http.Client{}
	//
	//form := url.Values{}
	//form.Add("ln", c.ln)
	//form.Add("ip", c.ip)
	//form.Add("ua", c.ua)
	//req.PostForm = form
	//req.Header.Add("Content-Type", "application/x-www-form-urlencoded")
	//req, err := http.NewRequest("POST", AddressURL, strings.NewReader(form.Encode()))
	//
	//glog.Info("form was %v", form)
	//resp, err := hc.Do(req)
}
